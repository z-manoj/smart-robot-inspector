# Smart Robot Inspector - ROS2 Dependencies
# Install with: pip install -r requirements-ros2.txt
# (After installing ROS2 from https://docs.ros.org/en/humble/Installation.html)

# Core ROS2
rclpy>=0.10.0
sensor-msgs>=0.0.5
std-msgs>=0.0.5

# Image processing
cv-bridge>=0.5.0
image-transport>=0.1.0

# Launch support
launch>=0.20.0
launch-ros>=0.20.0

# Optional visualization
# rviz2>=0.10.0

# Optional testing
# pytest>=7.0.0
# pytest-ros>=0.1.0
